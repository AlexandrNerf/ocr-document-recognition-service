# @package _global_

defaults:
  - _self_
  - data: ocr_datamodule # choose datamodule with `test_dataloader()` for evaluation
  - model: ocr
  - logger: tensorboard
  - trainer: gpu
  - paths: ocr_paths
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: /home/ovsyannikoviv/ocr_modeling/assets/models/crnn_lstm_aug.ckpt
