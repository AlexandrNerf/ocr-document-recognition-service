version: 1
formatters:
  simple:
    format: '[EVALUATE] - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    stream: ext://sys.stdout
root:
  handlers: [console]
  level: INFO

disable_existing_loggers: false